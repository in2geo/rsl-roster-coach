.vercel
.env
tools/output/
node_modules/
.env*
.claude/launch.json

# raid.guide scraper review artifacts (source of truth is Supabase)
output/

# Deep Blue loop generated output (regenerate with `node tools/loop.mjs`)
knowledge/loop-state.json
knowledge/pending-review.md
knowledge/model-accuracy.md
knowledge/calibration-proposals.md

# start-stack.ps1 service logs (app / reader / watch-reconcile / auto-profile-sync).
# Covers both <service>.log and <service>.err.log — runtime output, never source.
*.log
